import 'package:flutter/material.dart';

class AhmadView extends StatelessWidget {
  const AhmadView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Ahmad Zakria'),
    );
  }
}
