import 'package:flutter/material.dart';

class ShamsView extends StatelessWidget {
  const ShamsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
        child: Scaffold(
      body: Center(
        child: Text("Shams View"),
      ),
    ));
  }
}
